[project]
name = "yahoofparser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "deepblueseabbt", email = "deepbluesea.bbt@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "yfinance>=1.6.0",
]

[project.scripts]
yahoofparser = "yahoofparser:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
